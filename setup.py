from setuptools import setup, find_packages

setup(
    name="CustomLogger",
    version="1.5",
    packages=find_packages(),
)

# Run "pip install -e ."" to install the package in editable mode. 
# This will allow you to make changes to the package and see the changes reflected in your environment without having to reinstall the package.